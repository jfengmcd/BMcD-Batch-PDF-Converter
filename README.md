# BMcD-Batch-PDF-Converter
This tool will turn off track changes and any comments on the document before plotting to pdf so the final result is a clean file. It works on .doc/x and .xls/s/m files only. This tool also allows the user to include any PDF files and include in the final combination step.
